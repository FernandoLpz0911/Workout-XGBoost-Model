name: exercise_analyzer
description: "Exercise for analyzing exercises."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  provider: ^6.1.5+1
  file_picker: ^11.0.2
  shared_preferences: ^2.3.4
  fl_chart: ^0.70.2
  audioplayers: ^6.0.0
  vibration: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
